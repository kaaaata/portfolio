import { useState } from 'react';
import { css, jsx } from '@emotion/core'; /** @jsx jsx */
import { PLL } from './PLL';
import { FlexContainer, Spacer, Button } from '../particles';

export const PLLTrainer = () => {
  
  return (
    <section>
      ASDF
    </section>
  );
};
